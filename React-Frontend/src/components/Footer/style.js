import styled from 'styled-components';

export const FooterTag = styled.footer`
  position: relative;
  text-align: center;
  font-size: 1.6rem;
  background: #fff;
  z-index: 2;

  & a {
    color: #444;
  }

  & p {
    margin: 0;
    padding: 3rem;
  }
`;
